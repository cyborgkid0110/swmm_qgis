"""KPI — Flood Risk (FROI) computation package.

Subpackages:
  * ``weights``    — IFAHP + EWM + combined weight computation.
  * ``indicators`` — FHI, FEI, FVI, FRI indicator groups.
  * (top-level)    — ``aggregator`` (spatial mapping), ``froi`` (orchestrator).
"""
